# Linux/macOS cron example — runs daily at 08:00 local time.
# Install with: crontab -e
# Then paste the line below, updating the paths to your repo and venv.

0 8 * * * cd /path/to/startup-radar-template && /path/to/venv/bin/startup-radar run --scheduled >> logs/cron.log 2>&1
